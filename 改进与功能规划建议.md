# 改进与功能规划建议

> 针对 `D:\xiangmudaima` 全工作区的参谋建议。
> 结论基于对代码的实际核查（每条都标注了证据位置），不是泛泛而谈。
> 编写日期：2026-09-23

---

## 一、总体判断

这个工作区实际上是一条**完整的个人 AI 陪伴产品链路**，而不是零散的练习：

```
zjy2 产品方案 → zjy5 数据采集 → zjy7 模型微调 → zjy6/zjy8 量化部署 → zjy9 应用系统
```

其中 **zjy9 是唯一"活着"的系统**（约 1 万行，FastAPI + Android + 树莓派 + Web 面板 + 云端兜底），
其余五个是工具链或流水线。

**核心判断：现在的问题不是"功能不够"，而是"工程成熟度不足 + 安全边界没关上"。**

zjy9 的功能面已经很宽——REST/WebSocket 双通道、RAG 记忆、用户学习知识图谱、情感引擎、
6 家云端兜底、公网隧道。经过 2026-09-21 那轮修复（70 项），功能性 Bug 基本清完了。

所以下一步性价比最高的**不是加功能**，而是把 zjy9 从"能跑"推到"敢长期开着"。
下面第二节的三个洞，就是"能不能长期开着"的分水岭。

---

## 二、必须先修（正确性 / 安全）

### 2.1 ⚠️ WebSocket 完全没有鉴权，且日志流会广播聊天正文（高危）

这是我在核查中发现的、此前未被记录的问题，风险等级高于已修复的任何一项。

**证据链**（三处叠加才形成完整漏洞）：

1. 鉴权中间件的免认证前缀包含 `/ws`：
   `main.py:74-77` → `public_prefixes = ("/static", "/ws")`
2. 两个 WebSocket 端点自身不校验 token，只读 query 参数：
   - `main.py:965-973` → `/ws/chat` 只取 `device_id` / `device_name` / `session_id`
   - `main.py:1126-1132` → `/ws/logs` 直接 `add_log_observer(websocket)`
3. 日志服务会把**聊天正文**广播给所有日志观察者：
   `logger_service.py:179-183` → `_enqueue("broadcast", {"type": "chat", ..., "content": content[:200]})`

**后果**（尤其在开了 Cloudflare Tunnel 之后）：

| 攻击面 | 能做到什么 |
|---|---|
| `wss://<公网域名>/ws/logs` | **实时读到用户与 AI 的对话正文**（每条最多 200 字符） |
| `wss://<公网域名>/ws/chat` | 白嫖推理，且**绕过 `/api/chat` 的 60 次/分钟限流** |
| 两者叠加 | 设置了 `api_token` 也拦不住——门是锁了，但窗户开着 |

**建议改法**：

- WebSocket 握手时校验令牌：优先用 `Sec-WebSocket-Protocol` 携带（不落 URL 日志），
  或退而求其次用 query 参数 + 首次消息认证帧。
- **把聊天正文从广播里剥掉**：`/ws/logs` 只应传元数据（角色、长度、token 数、耗时），
  正文要么不传、要么只给已认证的面板会话。
- 给 `/ws/logs` 单独授权——它是运维观察通道，不该和聊天通道共用免认证策略。

> 这一项建议**优先于本文件其他所有条目**。

### 2.2 设备身份由客户端自报 → 多用户隔离可被绕过

**证据**：

- `main.py:972` → `device_id = websocket.query_params.get("device_id", "anonymous")`
- `main.py:573` → `learning_engine.use_device(req.device_id)`（REST 路径同理）
- `main.py:1186-1189` → `/api/learning/profile` 返回 `learning_engine.profile`，
  即"当前设备"的画像，**不要求任何凭证**

**后果**：`device_id` 是客户端随便填的字符串。改一下就能：
读别人的画像、往别人的知识图谱里写内容（画像投毒）。
README 里宣传的"多用户隔离"目前是**隔离了，但门没锁**。

**建议改法**：

- 把设备身份与凭证绑定：每个设备签发一个 token，服务端从 token 反解 `device_id`，
  不再信任请求里的自报值。
- 或者（更轻）在 `api_token` 非空时，`/api/learning/*` 与 `/ws/*` 都强制校验。

### 2.3 没有并发闸门，Ollama 会被打爆

**证据**：`main.py` 与 `ollama_client.py` 中**没有** `Semaphore` / `asyncio.Lock` / 并发上限。
`max_connections` 只限制 WebSocket 连接数，REST `/api/chat` 完全不设防。

**后果**：Ollama 本身是串行推理，多个并发请求会全部堆在服务端排队，
内存、超时、用户体验一起失控——而且**没有队列深度可见性**，出问题时只能看到"卡住了"。

**建议改法**：

- `asyncio.Semaphore` 限制同时生成数，做成运行时配置（如 `max_concurrent_generations`）。
- 队列满时快速失败：返回 `429` 或 `503` 并带 `retry_after`。
- 面板上展示"当前排队数 / 正在生成数"，让状态可见。

---

## 三、工程质量

### 3.1 测试跑不起来，所以没法上 CI

**现状**：`zjy9/server/tests/` 下四个脚本都是**独立脚本**，用各自的 `check()` 累加结果，
没有 pytest 入口。仓库里**没有** `.github/`、`pyproject.toml`、`pytest.ini`、`conftest.py`。

**影响**：74 项断言全靠人手一条条跑，改动多了必然漏。

**建议**：加 `pyproject.toml` + 把这些脚本包成 pytest 用例（或先加一个 `run_tests.py` 汇总入口），
再配一个 GitHub Actions 工作流跑 lint + 测试。**这是让后面所有改动"有网"的前提。**

### 3.2 依赖没有锁定

**现状**：`requirements.txt` 里核心依赖已 pin（fastapi/uvicorn/httpx/pydantic/websockets），
但 `scikit-learn>=1.3.0`、`psutil>=5.9.0` 是浮动版本，且没有 lock 文件。

**建议**：`pip freeze` 产出 `requirements.lock`，或引入 uv/poetry。
特别是 `scikit-learn` 直接决定 TF-IDF 记忆的检索行为，版本漂移会让记忆表现悄悄变化。

### 3.3 客户端零测试——而那里恰好是之前的高危区

**现状**：`zjy9/android`（约 1900 行 Kotlin）与 `zjy9/rpi_client` **没有任何测试**。
本机无 Android SDK，无法编译验证。

**建议**：Android 至少补**纯 JVM 单测**，覆盖三处正好是上一轮修的高危点：

| 待测逻辑 | 位置 | 为什么值得测 |
|---|---|---|
| `**粗体**` 解析器 | `ChatScreen.kt` | 曾是未闭合 `**` 死循环 → ANR |
| WebSocket 重连策略 | `WebSocketClient.kt` | 曾是计数被重置导致无限重连 |
| 分片串行分发顺序 | `ChatViewModel.kt` | 曾用并发 `launch` 导致丢字/串轮 |

这三处**都是纯逻辑、不依赖 Android SDK**——把算法抽到独立的 Kotlin 类里就能用 JUnit 覆盖，
是"没有真机也能拿到安全网"的最高性价比做法。

### 3.4 聊天记录没有导出与备份

**现状**：有 `/api/memory/export`（记忆），但**没有** `chat_sessions` / `chat_messages`
的导出接口，也没有任何数据库备份机制。只有按会话查询和单条删除。

**建议**：

- `GET /api/sessions/{id}/export?format=json|md` —— 导出单会话为可读文本。
- 定期 `VACUUM INTO` 备份 SQLite（启动时或定时任务）。

对一个"陪伴型"应用来说，**对话记录是最不该丢的资产**。

---

## 四、可以添加的功能

按"对体验的杠杆 / 实现成本"排序。

### A. 会话与记忆（最贴合角色扮演场景）

| 功能 | 价值 | 备注 |
|---|---|---|
| **长期记忆自动摘要** | 高 | 现在是"最近 20 条历史 + 检索片段"，长对话会丢早前内容。做分层记忆：短期窗口 / 中期摘要 / 长期事实 |
| **记忆单条删除 + 管理 UI** | 高 | 后端目前**没有单条删除接口**，清理只能整库重建（`clear` + 重新 `add_document`）。面板里可视化知识图谱并支持手动编辑 |
| **会话导出/导入** | 中 | 换设备、换机器时迁移 |
| **对话分支 / 重新生成对比** | 中 | Android 已有"重新生成"，但没有分支树，无法对比两个回复 |

### B. 角色与表达

| 功能 | 价值 | 备注 |
|---|---|---|
| **多角色卡** | 高 | 现在人设硬编码在 `elysia_prompt.py`。抽成可切换的角色卡（YAML/JSON），一套系统跑多个角色 |
| **情绪驱动的 TTS** | 高 | 情感引擎已经能给出 `dominant` / `intensity`，正好驱动语音朗读的语调 |
| **立绘/表情随情绪切换** | 中 | 情绪 → 图片映射，配合 Android 界面 |
| **语音输入（ASR）** | 中 | 补齐语音交互闭环 |
| **主动消息 / 定时问候** | 中 | `time_context` 已有时间感知，做"早安/晚安/久未联系"主动开口 |

### C. 模型与检索

| 功能 | 价值 | 备注 |
|---|---|---|
| **混合检索（BM25 + 向量）** | 高 | 现在默认纯 TF-IDF，召回质量有限。混合检索比直接上 ChromaDB 更划算 |
| **流式工具调用** | 中 | 让 AI 能查天气、看日程，甚至联动 `zjy5` 的 B 站下载器 |
| **模型健康探测与降级链** | 中 | 现在 `auto` 只判断 Ollama 是否 `running`，不检测模型是否真的可用/响应超时 |

### D. 工程与运维

| 功能 | 价值 | 备注 |
|---|---|---|
| **可观测性指标** | 高 | QPS、生成延迟、token/s、队列深度、记忆命中率。现在只有日志 |
| **容器化** | 中 | Dockerfile + compose，把服务 / Ollama / 隧道编排起来，部署才可复现 |
| **启动时配置校验** | 中 | 校验 `num_ctx` 与"人设 + Few-Shot + 记忆 + 画像 + 历史"的总注入量是否匹配——上一轮就是在这里出的上下文溢出 |

### E. 客户端

| 功能 | 价值 | 备注 |
|---|---|---|
| **正式 Web 聊天页** | 中 | 面板里的"聊天测试"比较简陋，可做成独立的正式前端 |
| **桌面端** | 低 | Tauri/Electron 复用同一 WS 协议，成本主要在维护 |

---

## 五、不建议现在做的

参谋也要说"不做什么"：

- **RK3568 / 嵌入式部署**：没有硬件、无法验证，`zjy6` 自己也声明了"未实现"。
  在没有实物之前投入产出比极低。
- **把 ChromaDB 设为默认记忆后端**：常驻约 400MB，对个人项目不划算。
  先把混合检索做好，收益更大、成本更低。
- **引入完整用户账号体系**：个人自用场景，**设备级 token 足够**。
  账号体系会带来注册/找回/权限一堆负担，收益却不匹配。
- **继续扩大功能面**：在 2.1 的洞补上之前，加的功能越多，暴露面越大。

---

## 六、建议推进顺序

| 阶段 | 内容 | 判断标准 |
|---|---|---|
| **第 1 步（边界）** | WS 鉴权 + 日志脱敏 + 并发闸门 + 启动配置校验 | 做完才敢长期开着、才敢开公网隧道 |
| **第 2 步（安全网）** | pytest 化 + CI + 依赖锁定 + Android 纯逻辑单测 | 后续改动有自动化兜底 |
| **第 3 步（数据主权）** | 记忆单条删除 + 记忆管理 UI + 会话导出 + 数据库备份 | 用户能掌控自己的数据 |
| **第 4 步（体感）** | 混合检索 + 长期记忆摘要 | "她记得住"的体感明显提升 |
| **第 5 步（体验）** | 多角色卡 + TTS + 主动消息 | 从"能用"到"想用" |
| **贯穿** | 可观测性指标 | 任何时候都能看清系统状态 |

---

## 七、缺口清单：完全缺失的能力

前六节讲的是「已有但可改进」与「可以新增」。这一节专门列**当前压根不存在**的东西——
不是做得不够好，而是完全没有。按「缺了会出事」的严重度排列。

### 7.1 数据演进能力（最危险的一类缺失）

**数据库没有任何迁移机制。**

证据：`database.py` 中无 `schema_version`、无 `PRAGMA user_version`、无 `ALTER TABLE`。

后果：4 张表目前是「一次建好、永不改变」的假设。一旦要加字段（比如给会话加标题、
给消息加已读状态），**老数据库不会自动升级**——要么启动报错，要么静默用错结构。
而这个库已经跑了几个月，里面是真实对话记录。

补充证据：`chat_sessions` 表没有 `title` 字段（第 85-94 行），所以会话列表只能显示
设备名和消息数，**无法给会话起名字**——这本身就是"想加字段却加不了"的现成例子。

**建议**：引入 `PRAGMA user_version` + 顺序迁移脚本（哪怕只有十几行），
在 `init_db()` 里按版本号逐级升级。这是投入产出比最高的一项补课。

### 7.2 容错能力

**两个模型客户端都没有重试。**

证据：`cloud_client.py` 与 `ollama_client.py` 中 retry / attempt 相关逻辑均为 0 处。

后果：

- 云端：一次网络抖动 = 整轮对话失败
- 本地：Ollama 冷启动加载模型要 10~30 秒，这个窗口最容易超时或连接失败，
  而恰恰没有任何重试——**最需要重试的地方最缺重试**

**其他缺失**：

| 缺失 | 说明 |
|---|---|
| 无降级链 | `auto` 只判断 Ollama 是否 `running`，不探测模型是否真可用、响应是否超时 |
| 无 `/ready` 就绪探针 | 只有 `/health`，无法区分"进程活着"与"能提供服务" |
| 无 `/metrics` | 无任何指标端点 |

### 7.3 客户端与后端的能力不对齐

**后端有 `/api/messages/search`，Android 端没有搜索入口。**

证据：`main.py` 中该接口存在；`android/app/src/main/java/**/*.kt` 中无任何搜索相关代码。

这是典型的「能力已经建好，但用户用不到」——接口等于白写。

**其他缺失**：

- Android 无会话导出
- Android 无真正的离线模式（只有连接状态指示，没有本地推理或离线消息队列）
- **本地那个 `app-debug.apk` 是 6 月 25 日构建的旧产物**，不含 9 月修的高危修复
  （`**粗体**` 解析器 ANR、无限重连、分片错序）。装上它等于装了个已知有 bug 的版本。
  而且没有 CI 产出新 APK，所以「改了但没构建」是当前常态
- `rpi_client` 无测试、无打包/发布方式

### 7.4 工程约束（"没有规矩"）

| 缺失 | 后果 |
|---|---|
| 无 lint/format 配置（ruff / flake8 / black / .editorconfig） | 风格靠自觉，改一次漂一次 |
| 无类型检查（mypy / pyright） | 第二节 2.1 那类类型错误只能靠运行时撞出来 |
| 无 pre-commit | 提交前无任何自动把关 |
| 无 CI | 74 项断言没有任何人/机器在跑 |
| 无依赖锁定 | 记忆检索行为会随 scikit-learn 版本悄悄漂移 |
| 无 LICENSE | 公开仓库却没有授权声明 |
| 无 CHANGELOG | 改动历史只能靠 git log，而远端是 API 推送、本地无 git 仓库 |

### 7.5 文档与代码脱节（已证实 3 处）

`zjy9/README.md` 与实际代码不符：

| README 写的 | 实际值 | 位置 |
|---|---|---|
| Few-Shot「5 组」 | **8 组**（16 条消息） | README:301 |
| 输出超「500 字符」截断 | **400** 字符 | README:276 |
| `test_regressions.py`「30 项」 | **41 项** | README:920 |

这不是笔误，而是**缺少「文档与代码一致性」检查机制**的结果。
建议：把关键数字抽成可从代码读出的常量，或加一条断言让测试校验 README 里的数字。

### 7.6 项目结构层面

- **根目录散落 3 个临时脚本**：`check_encoding.py`、`count_lines.py`、`fix_encoding.py`
  （虽已排除在推送外，但仍留在工作区里制造噪音）
- **空目录**：`.vscode/`、`zjy6/pc_server/`
- **`local.properties` 硬编码了他人机器的路径**（`C:\Users\431\AppData\Local\Android\Sdk`）——
  虽然被 gitignore 保护，但说明构建配置完全靠手工，换机器就断
- **GGUF 工具链分散在 zjy6 与 zjy8**，且历史上出现过互相矛盾的脚本
  （其中 `fix_gguf.py` 会写坏模型文件）。目前没有单一可信入口
- **无统一入口**：六个项目各跑各的，没有一条命令或一份文档把
  「采集 → 微调 → 量化 → 部署 → 服务」串起来
- **无跨项目共享代码**：GGUF 解析逻辑在 zjy6 / zjy8 各写了一遍

### 7.7 产品层面

- `zjy2`（次元萌盒）**完全未实现**，只有文档和示例代码
- 无多设备历史同步
- 无对话统计洞察（有会话计数，但没有"聊了多少天 / 话题分布 / 情绪走势"）
- 无 i18n

---

## 附：本文件涉及的关键证据位置

| 问题 | 文件:行 |
|---|---|
| WS 免认证前缀 | `zjy9/server/main.py:74-77` |
| `/ws/chat` 不校验 token | `zjy9/server/main.py:965-973` |
| `/ws/logs` 不校验 token | `zjy9/server/main.py:1126-1132` |
| 聊天正文被广播 | `zjy9/server/logger_service.py:179-183` |
| device_id 客户端自报 | `zjy9/server/main.py:972`、`main.py:573` |
| 画像接口无凭证要求 | `zjy9/server/main.py:1186-1189` |
| 无并发闸门 | `main.py` / `ollama_client.py` 全文件无 Semaphore/Lock |
| 无 CI / 无 pytest 入口 | 仓库无 `.github/`、`pyproject.toml`、`pytest.ini` |
| 依赖未锁定 | `zjy9/server/requirements.txt` |
| 客户端零测试 | `zjy9/android`、`zjy9/rpi_client` 下无 test 文件 |
| 无聊天记录导出 | 仅有 `/api/memory/export`（`main.py:1377`） |
| 数据库无迁移机制 | `zjy9/server/database.py` 全文件无 `schema_version` / `user_version` / `ALTER TABLE` |
| 会话表无 title 字段 | `zjy9/server/database.py:85-94` |
| 消息表无保留策略 | `database.py` 仅有 `trim_system_logs`（:467），`chat_messages` 无裁剪 |
| 两个模型客户端均无重试 | `zjy9/server/cloud_client.py`、`ollama_client.py`（retry 命中 0 处） |
| 无 `/ready`、无 `/metrics` | `zjy9/server/main.py` 仅有 `/health`（:295） |
| Android 无消息搜索入口 | `zjy9/android/app/src/main/java/**/*.kt`（无 search 相关代码） |
| 旧 APK 产物 | `zjy9/android/app/build/outputs/apk/debug/app-debug.apk`（6-25 构建） |
| `local.properties` 指向他人机器 | `zjy9/android/local.properties` → `C:\Users\431\...` |
| 无工程配置 | 无 `LICENSE` / `.editorconfig` / `ruff.toml` / `setup.cfg` |
| README 数字过期 | `zjy9/README.md:276`（500→400）、`:301`（5 组→8 组）、`:920`（30→41 项） |
